import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Next.js ไม่มีฟีเจอร์ server: { open: true } ที่ใช้เปิดเบราว์เซอร์โดยอัตโนมัติ
};

export default nextConfig;
